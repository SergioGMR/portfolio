<footer class="border-t py-6 md:px-8 md:py-0">
  <div
    class="container flex flex-col items-center justify-between gap-4 md:h-24 md:flex-row"
  >
    <p
      class="text-center text-sm leading-loose text-muted-foreground md:text-left"
    >
      <span id="from"></span>
      {' '}
      <a
        href="https://github.com/BUE221"
        target="_blank"
        rel="noreferrer"
        class="font-medium underline underline-offset-4"
      >
        BUE221.
      </a>
      <span id="by"></span>
      {' '}
      <a
        href="https://github.com/sergiogmr"
        target="_blank"
        rel="noreferrer"
        class="font-medium underline underline-offset-4"
      >
        SergioGMR
      </a>
      <span id="source"></span>
      {' '}

      <a
        href="https://github.com/sergiogmr/portfolio"
        target="_blank"
        rel="noreferrer"
        class="font-medium underline underline-offset-4"
      >
        GitHub.
      </a>
    </p>
  </div>
</footer>

<script>
  const footerTextsTypes = {
    es: {
      from: 'string',
      by: 'string',
      source: 'string',
    },
    en: {
      from: 'string',
      by: 'string',
      source: 'string',
    },
  }
  const footerTexts: typeof footerTextsTypes = {
    es: {
      from: '"Bifurcado" de',
      by: 'Modificado con 💜 usando Astro y Tailwind CSS por',
      source: 'El código fuente está disponible en',
    },
    en: {
      from: '"Forked" from',
      by: 'Modded with 💜 using Astro and Tailwind CSS by',
      source: 'The source code is available on',
    },
  }
  const updateTexts = () => {
    const language = localStorage.getItem('language') as 'es' | 'en'
    const texts = footerTexts[language]
    ;(document.querySelector('#from')! as HTMLElement).innerText = texts.from
    ;(document.querySelector('#by')! as HTMLElement).innerText = texts.by
    ;(document.querySelector('#source')! as HTMLElement).innerText =
      texts.source
  }

  if (typeof window !== 'undefined') {
    window.addEventListener('languageChange', updateTexts)
    document.addEventListener('astro:after-swap', updateTexts)
    updateTexts()
  }
</script>
